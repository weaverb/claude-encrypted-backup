[Unit]
Description=Weekly encrypted-backup run

[Timer]
OnCalendar=Sun *-*-* 03:00:00
RandomizedDelaySec=15min
# Fires at next boot/login if the machine was off/asleep at the scheduled time.
Persistent=true

[Install]
WantedBy=timers.target
